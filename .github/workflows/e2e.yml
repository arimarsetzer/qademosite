name: Cypress Regression Tests

on:
  push:
    branches: ['main']
  workflow_dispatch:
    inputs:
      cli:
        description: Run by cypress command line
        default: npm test

permissions:
  contents: read
  checks: write

jobs:
  cypress:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Check formatting
        run: npm run format:check

      - name: Run Cypress suite (Chrome, headless)
        id: cypress
        uses: cypress-io/github-action@v6
        with:
          install: false
          command: ${{ inputs.cli || 'npm test' }}

      - name: Publish JUnit report
        if: ${{ !cancelled() && steps.cypress.outcome != 'skipped' }}
        uses: mikepenz/action-junit-report@v6
        with:
          report_paths: cypress/reports/junit/*.xml
          check_name: Cypress JUnit Report
          require_tests: true
          include_passed: true
          detailed_summary: true

      - name: Upload JUnit XML
        if: ${{ !cancelled() && steps.cypress.outcome != 'skipped' }}
        uses: actions/upload-artifact@v4
        with:
          name: cypress-junit-report
          path: cypress/reports/junit
          retention-days: 14

      - name: Upload failure screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: cypress/screenshots
          retention-days: 14
